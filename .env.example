# =============================================================================
# TrustNet AI — Master Environment Configuration Template (.env.example)
# Copy this file to .env and configure values for your local environment.
# =============================================================================

# --- General & Logging ---
ENVIRONMENT=dev
LOG_LEVEL=INFO

# --- Service Ports ---
GATEWAY_PORT=8000
AUTH_SERVICE_PORT=8001
SCAN_SERVICE_PORT=8002
IMAGE_SERVICE_PORT=8003
TRUST_ENGINE_PORT=8004
FRONTEND_PORT=5173

# --- Security / JWT Authentication ---
JWT_SECRET_KEY=trustnet_super_secret_jwt_key_for_development_purposes_only
JWT_ALGORITHM=HS256
JWT_ACCESS_EXPIRY_MINUTES=30
JWT_REFRESH_EXPIRY_DAYS=7

# --- Messaging: Apache Kafka (KRaft mode) ---
KAFKA_BOOTSTRAP_SERVERS=localhost:9094
KAFKA_CONSUMER_GROUP_PREFIX=trustnet_
KAFKA_CONSUMER_GROUP_IMAGE=trustnet_image_deepfake_group
KAFKA_CONSUMER_GROUP_TRUST=trustnet_trust_engine_group

# --- Relational Database (PostgreSQL / SQLite fallback) ---
POSTGRES_USER=trustnet
POSTGRES_PASSWORD=trustnet_dev_password
POSTGRES_DB=trustnet_dev
DATABASE_URL=postgresql+asyncpg://trustnet:trustnet_dev_password@localhost:5432/trustnet_dev

# --- Document Store (MongoDB) ---
MONGO_URL=mongodb://localhost:27017/trustnet_dev

# --- In-Memory Cache (Redis) ---
REDIS_URL=redis://localhost:6379/0

# --- Object Storage (MinIO / S3 / Local fallback) ---
STORAGE_DIR=./storage_uploads
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadminpassword
MINIO_BUCKET_NAME=trustnet-uploads
MINIO_SECURE=false

# --- Downstream Microservice URLs for API Gateway ---
AUTH_SERVICE_URL=http://localhost:8001
SCAN_SERVICE_URL=http://localhost:8002
IMAGE_SERVICE_URL=http://localhost:8003
TRUST_ENGINE_URL=http://localhost:8004

# --- Hugging Face AI Integration ---
# 1. Get your free read token at: https://huggingface.co/settings/tokens
# 2. Paste your token below:
HUGGINGFACE_API_KEY=hf_your_token_here
HF_DEEPFAKE_MODEL=dima806/deepfake_vs_real_image_detection
